#include <stdio.h>
#include <stddef.h>
#include <string.h>
size_t		ft_strlcat(char *dest, const char *src, size_t n);

int main()
{
	char str2[50] = "That";
	char str3[] = "Yelloland";

	strlcar(str2, str3, 50);
	return 0;
}
